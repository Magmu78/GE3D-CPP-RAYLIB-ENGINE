#pragma once

#include <math.h>
#include "raylib.h"

/* bool CheckCollisionPointSphereAlternative(Vector3 point, Vector3 center, float radius)
{
    float distance = pow(point.x - center.x, 2) + pow(point.y - center.y, 2) + pow(point.z - center.z, 2);
    return distance <= pow(radius, 2);
} */